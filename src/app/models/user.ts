export interface User {

    /* ?: - significa que o id no caso, não é obrigatório, pode ser number ou undefined */
    id: number,
    nome: string,
    sobrenome: string,
    idade: number,
    profissao: string
}
